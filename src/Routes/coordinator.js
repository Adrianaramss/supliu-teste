export const goToHomePage = (navigator) => {
    navigator('/')
}

export const goToAdicionar = (navigator) => {
    navigator('/adicionar')
}

export const goToDeletar = (navigator) => {
    navigator('/deletar')
}
